[project]
name = "django-polls-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=6.0.4",
]

[dependency-groups]
dev = [
    "black>=26.3.1",
]
